# requirements.txt — 교통 STGNN 경량 의존성
# =====================================================================
# ⚠️ 과제 01(OccWorld)의 무거운 mmdet3d/mmcv/spconv 스택과 **분리**한다.
#    소형 STGNN 이라 게이밍 PC(RTX 3060 8GB)에서 실제 학습 가능(순수 pip 로 충분).
#
# 검증 표기: [확정] = Phase 2 에서 실제 설치·구동 확인 / [후보] = 미검증
#
# ── Phase 2 에서 실측 확정한 환경 (2026-07-04, RTX 3060 8GB) ─────────────
#   OS: Windows 11, Python 3.13 (Anaconda base), CUDA 드라이버 12.9
#   torch 2.6.0+cu124 설치 명령(실제 사용, 성공):
#     pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#   → torch.cuda.is_available()=True, device=RTX 3060(sm_86), GPU-vs-CPU 수치 일치 확인.
#   ⚠️ Windows + Anaconda(MKL libiomp5) + pip torch 는 OpenMP DLL 중복 충돌이 난다.
#      스크립트가 KMP_DUPLICATE_LIB_OK=TRUE 를 자동 설정(결과 정확성 영향 없음 확인).
#   CPU 폴백: CUDA 미탐지 시 스크립트가 자동 CPU 사용(소형이라 CPU 로도 구동 가능).
#   ※ 초기 Phase 0 의 torch==2.2.2 핀은 Python 3.13 용 휠이 없어 폐기(→ 2.6.0 로 확정).
# =====================================================================

# --- 딥러닝 코어 [확정] ---
# GPU: 위 --index-url(cu124) 로 설치. CPU 전용은 기본 PyPI 휠(pip install torch==2.6.0).
torch==2.6.0

# --- 과학 스택 [확정] (Phase 1 기준선·전처리에서 실사용, py3.13 base) ---
numpy==2.3.5
pandas==2.3.3
h5py==3.15.1
tables==3.10.2          # pandas.read_hdf(.h5) 백엔드 (PyTables)
pyyaml==6.0.3
scipy==1.16.3           # 인접행렬(sparse)·거리 처리

# --- 학습 유틸 [후보] (선택; 미설치 시 학습 스크립트가 없어도 되게 처리) ---
tqdm==4.67.1            # 진행바(선택)
tensorboard==2.19.0    # 학습 곡선 로깅(선택)
